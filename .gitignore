# ── Дані корпусу (НЕ публікуються — авторські права Larian Studios) ──────────
# Повний корпус і проміжні файли — Google Drive (посилання у README)
data/
Localization/
*.xml

# JSONL-файли з ігровим текстом — не публікуються
# Виняток: examples/*.jsonl — малі ілюстративні вибірки (10-20 записів)
*.jsonl
!examples/*.jsonl

# ── Секрети ───────────────────────────────────────────────────────────────────
.env
*.env
*.key

# ── Логи та кеш ───────────────────────────────────────────────────────────────
logs/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ── Кеш моделей ML ────────────────────────────────────────────────────────────
.cache/
*.pt
*.bin

# ── Python virtual environments ───────────────────────────────────────────────
venv/
.venv/
env/
.env/

# ── Інструменти розробки ──────────────────────────────────────────────────────
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.DS_Store
Thumbs.db
